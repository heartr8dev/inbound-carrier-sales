# Optional overrides — defaults work for the docker-compose stack (nginx proxies /api/).
# Set VITE_API_BASE_URL if running `npm run dev` against the host API.
VITE_API_BASE_URL=/api/v1
VITE_API_KEY=devkey-please-change
